" Syntax highlight
syntax on

" Solorized theme. Get from https://github.com/altercation/vim-colors-solarized
"colorscheme solarized

" jj for ESC
imap jj <Esc>

" Set tabs to 2 spaces
set tabstop=2
set shiftwidth=2
set expandtab

" Ignore case for searches
set ignorecase

" Unless you type an uppercase letter
set smartcase

" Incremental searching is sexy
set incsearch

" Highlight things that we find with the search
set hlsearch
